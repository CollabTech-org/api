generator client {
  provider = "prisma-client-js"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "erd.svg"
  theme    = "neutral"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}
